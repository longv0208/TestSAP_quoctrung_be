name: CI

on:
  push:
    branches: [master, dev, dev1]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore -c Release

      # Test dùng EF InMemory (xem FURPMS.Tests) → không cần SQL Server trong CI.
      - name: Test
        run: dotnet test --no-build -c Release --verbosity normal
